import 'package:flutter/material.dart';

class starredReopsPage extends StatelessWidget {
  const starredReopsPage({Key? Key}) : super(key: Key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
